#ansible.cfg

[defaults]
inventory = ./hosts-dev
remote_user = ec2-user
private_key_file = ~/.ssh/ansible-course-key-pair.pem
host_key_checking = False
retry_files_enabled = False # any tiome a playbook fales it won't create a replay file


